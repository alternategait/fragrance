<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Todo List: </h1> <!-- h1 is the most important of headings there should be only one per page -->
    <ul class="todoItems"> <!-- the start of an unordered list with a class of todoItems  -->
    <% for(let i=0; i < products.length; i++) {%> <!-- this is the javascript that is embeded; we're intiating a for loop  -->
        <li class="item"><!-- this is a list item with the class of item-->
            <% if(products[i].completed === true) {%> <!-- if something in the item array with key of i has a completed that is true -->
                <span class='completed'><%= products[i].name %></span> <!-- add  the text of the items.thing into this span (meant for a small bit of text) and give it class completed -->
            <% }else{ %> <!-- if something in the item array with key of i has a completed of anything except true -->
                <span><%= products[i].name %></span><!-- add  the text of the items.thing into this span (meant for a small bit of text) -->
            <% } %>    
            <span class='fa fa-trash'></span> <!-- another span with class of fa fa-trash (very likely to give an icon using font awesome) -->
        </li> <!-- end the list item -->
    <% } %> <!-- end the embedded javascrip -->
    </ul><!-- end the unordered list -->

    <script src='js/main.js'></script> <!-- where to find the client side js -->
</body>
</html>
